import React from 'react';

import { AppTitle } from '../AppTitle/AppTitle';

import './Footer.scss';

export const Footer = () => (
  <footer className='footer container'>
    <AppTitle />
  </footer>
);
